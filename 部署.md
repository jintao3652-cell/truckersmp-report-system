# TruckersMP Report System 部署指南

本文适用于 Ubuntu 22.04/24.04、Debian 12。当前推荐架构：

- 主站：`https://reports.cnrpg.top:8335`
- 上传站：`https://upload.cnrpg.top:17023`
- Gunicorn：`127.0.0.1:8000`
- 数据库：MariaDB/MySQL 或 PostgreSQL
- 视频：本地 `VIDEO_FOLDER` 或 S3/MinIO

主站负责登录、注册、审核和页面；上传站只代理 `/api/v1/uploads`，两者共享同一个应用、数据库和存储。

## 1. 安装依赖

```bash
sudo apt update
sudo apt install -y git python3 python3-venv python3-dev build-essential nginx ffmpeg libmagic1 mariadb-server
```

确认 `python3 --version`、`ffprobe -version` 和 `nginx -v` 可用。

## 2. 数据库

```bash
sudo mysql
```

```sql
CREATE DATABASE truckersmp_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'truckersmp'@'127.0.0.1' IDENTIFIED BY 'CHANGE_ME_STRONG_PASSWORD';
GRANT ALL PRIVILEGES ON truckersmp_db.* TO 'truckersmp'@'127.0.0.1';
FLUSH PRIVILEGES;
EXIT;
```

## 3. 安装项目

```bash
sudo mkdir -p /var/www
sudo git clone https://github.com/jintao3652-cell/truckersmp-report-system.git /var/www/truckersmp-report-site
sudo chown -R www-data:www-data /var/www/truckersmp-report-site
cd /var/www/truckersmp-report-site
sudo -u www-data python3 -m venv .venv
sudo -u www-data .venv/bin/pip install -r requirements.txt
```

## 4. 存储目录

```bash
sudo mkdir -p /var/lib/truckersmp/uploads /sdk/truckersmp-videos
sudo chown -R www-data:www-data /var/lib/truckersmp /sdk/truckersmp-videos
sudo chmod 750 /var/lib/truckersmp /var/lib/truckersmp/uploads /sdk/truckersmp-videos
```

不要把视频目录配置为 Nginx 公共静态目录，只允许应用鉴权后通过 `X-Accel-Redirect` 读取。

## 5. 环境变量

```bash
cd /var/www/truckersmp-report-site
sudo cp .env.example .env
sudo nano .env
```

至少修改：

```dotenv
ENVIRONMENT=production
SECRET_KEY=生成一个随机长密钥
DATABASE_URL=mysql+pymysql://truckersmp:CHANGE_ME_STRONG_PASSWORD@127.0.0.1:3306/truckersmp_db?charset=utf8mb4
VIDEO_FOLDER=/sdk/truckersmp-videos
UPLOAD_FOLDER=/var/lib/truckersmp/uploads
PUBLIC_BASE_URL=https://reports.cnrpg.top:8335
UPLOAD_BASE_URL=https://upload.cnrpg.top:17023
UPLOAD_CORS_ORIGIN=https://reports.cnrpg.top:8335
UPLOAD_ALLOW_SESSION=1
SESSION_COOKIE_DOMAIN=.cnrpg.top
SESSION_COOKIE_SECURE=1
AUTO_CREATE_DB=0
API_REQUIRE_BEARER=1
MEDIA_PROCESSING_ASYNC=1
REQUIRE_REAL_MIME=1
REQUIRE_FFPROBE=1
METRICS_REQUIRE_AUTH=1
METRICS_TOKEN=另一个随机长令牌
```

生产环境不要使用默认密钥。若不希望跨子域共享 Cookie，可将 `SESSION_COOKIE_DOMAIN` 留空，并让上传前端使用 Bearer Token。

```bash
sudo chown www-data:www-data .env
sudo chmod 600 .env
```

## 6. 数据库迁移

```bash
cd /var/www/truckersmp-report-site
sudo -u www-data .venv/bin/flask --app run.py db upgrade
```

确认迁移链完整：

```bash
sudo -u www-data .venv/bin/python check_migrations.py
```

## 7. Gunicorn systemd

复制并编辑 `deploy/truckersmp-report-site.service`，确认工作目录、虚拟环境和 `EnvironmentFile` 路径正确：

```bash
sudo cp deploy/truckersmp-report-site.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now truckersmp-report-site
sudo systemctl status truckersmp-report-site
```

应用只监听 `127.0.0.1:8000`，不要直接暴露到公网。

## 8. Nginx 双域名

使用仓库中的 `deploy/nginx-dual-host.conf.example`，复制到 Nginx 配置目录后修改证书路径和上游地址：

```bash
sudo cp deploy/nginx-dual-host.conf.example /etc/nginx/sites-available/truckersmp
sudo ln -s /etc/nginx/sites-available/truckersmp /etc/nginx/sites-enabled/truckersmp
sudo nginx -t
sudo systemctl reload nginx
```

配置必须包含：

- `reports.cnrpg.top:8335` 代理所有主站请求到 `127.0.0.1:8000`。
- `upload.cnrpg.top:17023` 只代理 `/api/v1/uploads`。
- 上传站 `client_max_body_size 2048m`、`proxy_request_buffering off`、长超时。
- 主站 `/protected-videos/` 使用 `internal` 和 `alias /sdk/truckersmp-videos/`。

## 9. DNS 与 HTTPS

为两个域名分别添加 A/AAAA 记录指向服务器，并确保防火墙放行 TCP `8335`、`17023`（以及证书签发需要的 80/443）。证书可以使用 Certbot：

```bash
sudo apt install -y certbot
sudo certbot certonly --nginx -d reports.cnrpg.top -d upload.cnrpg.top
```

证书路径应与 Nginx 配置一致。若前端页面是 HTTPS，上传域也必须使用 HTTPS，否则浏览器会阻止混合内容。

## 10. 清理和媒体 Worker

```bash
sudo cp deploy/truckersmp-cleanup.service deploy/truckersmp-cleanup.timer /etc/systemd/system/
sudo cp deploy/truckersmp-media-worker.service deploy/truckersmp-media-worker.timer /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now truckersmp-cleanup.timer truckersmp-media-worker.timer
```

确认 `EnvironmentFile` 指向同一份 `.env`，否则 Worker 可能使用不同数据库或视频目录。

## 11. 验证清单

```bash
curl -I https://reports.cnrpg.top:8335/
curl -I https://upload.cnrpg.top:17023/api/v1/uploads
sudo journalctl -u truckersmp-report-site -n 100 --no-pager
sudo systemctl list-timers
```

浏览器验证：注册、登录、语言切换、上传页选择文件、分片上传进度、审核、分享播放、删除和过期清理。开发者工具中确认分片请求发往 `upload.cnrpg.top:17023`，响应包含正确的 CORS 头。

## 12. 常见问题

- CORS 错误：检查 `UPLOAD_CORS_ORIGIN` 是否与浏览器地址完全一致，包含协议和端口。
- 401：检查 `SESSION_COOKIE_DOMAIN`、HTTPS、Cookie 的 `Secure` 设置；生产 API 也可使用 Bearer Token。
- 413：同时检查 Nginx `client_max_body_size`、Flask `MAX_CONTENT_LENGTH` 和上传站端口配置。
- 播放 404：检查 `VIDEO_FOLDER`、Nginx `alias`、目录权限以及 `X-Accel-Redirect` 路径。
- Worker 不工作：检查 timer、service 的 `EnvironmentFile`、FFmpeg/ffprobe 路径和日志。
