# TruckersMP 举报视频系统部署教程

本文适用于 Ubuntu 22.04/24.04、Debian 12 等 systemd Linux 服务器。示例路径为 `/var/www/truckersmp-report-site`，运行用户为 `www-data`，域名为 `reports.example.com`。如果你的路径、域名或数据库不同，请全文替换后再执行。

## 1. 部署架构

生产环境推荐使用以下结构：

```text
浏览器
  -> HTTPS Nginx
  -> Gunicorn（127.0.0.1:8000）
  -> MySQL/PostgreSQL
  -> 本地 VIDEO_FOLDER 或 S3/MinIO
```

Nginx 负责 TLS、静态文件和大视频传输；Flask 只负责鉴权、审核和生成 `X-Accel-Redirect` 或 S3 签名 URL。

## 2. 准备服务器

```bash
sudo apt update
sudo apt install -y git python3 python3-venv python3-dev build-essential \
  nginx ffmpeg libmagic1 mariadb-server
```

确认版本：

```bash
python3 --version
ffmpeg -version
ffprobe -version
nginx -v
```

如果使用 PostgreSQL，请将 `mariadb-server` 换为 `postgresql postgresql-contrib`，并将数据库连接改成 PostgreSQL 驱动格式。

## 3. 创建数据库

### MariaDB/MySQL

```bash
sudo mysql
```

```sql
CREATE DATABASE truckersmp_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'truckersmp'@'127.0.0.1' IDENTIFIED BY '替换为高强度密码';
GRANT ALL PRIVILEGES ON truckersmp_db.* TO 'truckersmp'@'127.0.0.1';
FLUSH PRIVILEGES;
EXIT;
```

## 4. 下载代码并创建虚拟环境

```bash
sudo mkdir -p /var/www
sudo git clone https://github.com/jintao3652-cell/truckersmp-report-system.git /var/www/truckersmp-report-site
sudo chown -R "$USER":"$USER" /var/www/truckersmp-report-site
cd /var/www/truckersmp-report-site
python3 -m venv .venv
. .venv/bin/activate
python -m pip install --upgrade pip wheel
pip install -r requirements.txt
```

如果系统使用 `python-magic`，必须保留 `libmagic1`。如果启用 S3/MinIO，`boto3` 已由 requirements 安装。

## 5. 创建运行目录和权限

```bash
sudo mkdir -p /var/lib/truckersmp/uploads /sdk/truckersmp-videos
sudo chown -R www-data:www-data /var/lib/truckersmp /sdk/truckersmp-videos
sudo chmod 750 /var/lib/truckersmp /sdk/truckersmp-videos
sudo chmod 750 /var/lib/truckersmp/uploads
```

不要把 `/sdk/truckersmp-videos` 配置为 Nginx 的公开静态目录；只能通过 `internal` location 访问。

## 6. 配置环境变量

```bash
cd /var/www/truckersmp-report-site
sudo cp .env.example .env
sudo chown www-data:www-data .env
sudo chmod 600 .env
```

生成密钥：

```bash
python -c "import secrets; print(secrets.token_urlsafe(48))"
```

编辑 `.env`：

```env
SECRET_KEY=上面生成的随机值
ENVIRONMENT=production
AUTO_CREATE_DB=0
DATABASE_URL=mysql+pymysql://truckersmp:数据库密码@127.0.0.1:3306/truckersmp_db?charset=utf8mb4
UPLOAD_FOLDER=/var/lib/truckersmp/uploads
VIDEO_FOLDER=/sdk/truckersmp-videos
FFPROBE_PATH=/usr/bin/ffprobe
FFMPEG_PATH=/usr/bin/ffmpeg
REQUIRE_FFPROBE=1
REQUIRE_REAL_MIME=1
MEDIA_ACCEL_REDIRECT=1
SESSION_COOKIE_SECURE=1
API_REQUIRE_BEARER=1
METRICS_REQUIRE_AUTH=1
METRICS_TOKEN=另一个随机值
MEDIA_PROCESSING_ASYNC=1
```

### 邮件配置

自定义 SMTP：

```env
MAIL_PROVIDER=custom
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=1
MAIL_USERNAME=notice@example.com
MAIL_PASSWORD=SMTP授权码
MAIL_DEFAULT_SENDER=notice@example.com
```

也可使用 `gmail`、`outlook`、`qq`、`163`、`aliyun` 预设。邮件配置用于注册通知、重置密码、审核结果和分享查看提醒。

### 分享查看提醒

```env
SHARE_VIEW_NOTIFY_COOLDOWN_SECONDS=3600
```

同一个视频在冷却窗口内只发送一次提醒，避免重复刷新造成邮件轰炸。

### S3/MinIO

```env
STORAGE_BACKEND=s3
STORAGE_S3_BUCKET=truckersmp-videos
STORAGE_S3_ENDPOINT=https://s3.example.com
STORAGE_S3_REGION=us-east-1
STORAGE_S3_PREFIX=videos
STORAGE_SIGNED_URL_SECONDS=900
```

运行用户必须通过 AWS 环境变量、实例角色或 `~www-data/.aws/credentials` 获得访问权限。启用 S3 后，媒体播放、缩略图、删除、过期清理和 worker 都使用对象 key。

## 7. 执行数据库迁移

不要在生产使用 `db.create_all()`。执行完整 Alembic 链：

```bash
cd /var/www/truckersmp-report-site
sudo -u www-data .venv/bin/flask --app manage.py db upgrade
sudo -u www-data .venv/bin/python check_migrations.py
```

当前迁移应到 `0016_share_view_notice`。如果命令失败，先检查 `.env`、数据库权限和数据库驱动，不要手工跳过迁移。

## 8. 创建管理员

先注册普通账号：

```text
https://reports.example.com/register
```

然后在数据库中提升角色：

```sql
UPDATE user SET is_admin=1, role='admin' WHERE email='admin@example.com';
```

管理员可以审核、删除视频、管理用户、调整配额、撤销 Token 和控制分享。

## 9. Gunicorn 配置

确认 `gunicorn.conf.py` 存在并与 service 中的命令一致。推荐监听本机：

```python
bind = "127.0.0.1:8000"
workers = 2
threads = 4
timeout = 3600
graceful_timeout = 30
accesslog = "-"
errorlog = "-"
```

## 10. 安装 systemd 服务

```bash
sudo cp deploy/truckersmp-report-site.service /etc/systemd/system/
sudo cp deploy/truckersmp-cleanup.service deploy/truckersmp-cleanup.timer /etc/systemd/system/
sudo cp deploy/truckersmp-media-worker.service deploy/truckersmp-media-worker.timer /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now truckersmp-report-site.service
sudo systemctl enable --now truckersmp-cleanup.timer
sudo systemctl enable --now truckersmp-media-worker.timer
```

查看状态：

```bash
systemctl status truckersmp-report-site
systemctl status truckersmp-cleanup.timer
systemctl status truckersmp-media-worker.timer
journalctl -u truckersmp-report-site -f
```

定时清理会删除过期视频、缩略图和 S3 对象；media worker 会处理 ffprobe、时长和缩略图任务。

## 11. 配置 Nginx

复制示例并修改域名：

```bash
sudo cp deploy/nginx.conf /etc/nginx/sites-available/truckersmp-report-site
sudo ln -s /etc/nginx/sites-available/truckersmp-report-site /etc/nginx/sites-enabled/truckersmp-report-site
sudo nginx -t
sudo systemctl reload nginx
```

必须确认：

```nginx
location /protected-videos/ {
    internal;
    alias /sdk/truckersmp-videos/;
}
```

`alias` 必须和 `VIDEO_FOLDER` 完全一致，`internal` 不能删除。

## 12. 配置 HTTPS

先让域名解析到服务器，然后：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d reports.example.com
```

也可以参考 `deploy/nginx-https.conf.example` 手工配置。确认：

```bash
curl -I https://reports.example.com/health
```

生产环境必须使用 `SESSION_COOKIE_SECURE=1`，并确保 Nginx 传递 `X-Forwarded-Proto`。

## 13. 首次健康检查

```bash
curl -i https://reports.example.com/health
```

预期 JSON 中 `status` 为 `ok`。如果是 `degraded`，检查 `VIDEO_FOLDER` 是否存在且可写。

Metrics 默认需要独立 Header：

```bash
curl -H "X-Metrics-Token: 你的METRICS_TOKEN" https://reports.example.com/metrics
```

不要把 metrics 直接暴露给公网；优先在 Nginx 加 IP 白名单或仅允许 Prometheus 内网访问。

## 14. 上传和分享验证

1. 注册两个用户。
2. 用户 A 上传一个真实 MP4/MOV 文件。
3. 确认视频状态为 `pending`，普通公开列表不可见。
4. 管理员审核通过。
5. 用户 A 点击“分享视频”，复制分享链接。
6. 退出登录后打开分享链接，确认无需登录可以播放。
7. 检查用户 A 邮箱是否收到分享查看提醒。
8. 在冷却时间内重复打开，确认不会重复发信。
9. 管理员撤销分享，再次访问应返回 404。

## 15. 分片上传验证

启用 `MEDIA_PROCESSING_ASYNC=1` 后，上传页面可以选择“使用分片上传”。验证：

- 上传进度持续更新；
- 临时断网后重新上传能够从 offset 继续；
- 分片完成后生成 `MediaJob`；
- worker 执行后补齐 duration 和 thumbnail；
- 超过配额时不会创建正式视频；
- 过期会话由清理脚本删除。

## 16. 备份与恢复演练

本地 SQLite/本地视频：

```bash
sudo -u www-data .venv/bin/python backup.py /var/backups/truckersmp
sudo -u www-data .venv/bin/python restore_backup.py /var/backups/truckersmp/truckersmp-backup-文件名.tar.gz --target /var/lib/truckersmp-restore
```

生产数据库是 MySQL/PostgreSQL 时，使用数据库原生工具：

```bash
mysqldump --single-transaction -u truckersmp -p truckersmp_db | gzip > truckersmp.sql.gz
```

备份至少包含数据库、`.env` 安全副本、视频目录或对象存储配置。每月至少进行一次恢复演练，不能只确认备份文件存在。

## 17. 审计链校验

```bash
sudo -u www-data .venv/bin/python verify_audit.py
```

输出 `Audit chain OK` 才表示审核审计哈希链连续。发现断链时立即保留数据库副本并检查是否有人修改过审计表。

## 18. Prometheus 告警

将 `deploy/prometheus-alerts.yml` 加入 Prometheus 配置，并让 Prometheus 使用 `X-Metrics-Token` 请求头抓取 `/metrics`。重点指标：

- `truckersmp_videos_pending`：待审核数量；
- `truckersmp_disk_used_percent`：视频磁盘使用率；
- `truckersmp_disk_alert`：达到阈值；
- `truckersmp_login_failures_total`：登录失败总数。

## 19. 常见故障

### 502 Bad Gateway

```bash
systemctl status truckersmp-report-site
journalctl -u truckersmp-report-site -n 100 --no-pager
ss -lntp | grep 8000
```

### 上传后无法播放

检查 `ffprobe`、`VIDEO_FOLDER` 权限、Nginx `alias`、`X-Accel-Redirect` 和 `MEDIA_ACCEL_REDIRECT`。

### 邮件不发送

检查 SMTP 主机、端口、TLS、授权码、发件人和服务器出站防火墙；日志会记录 `Email not sent` 或 SMTP 异常。

### S3 播放失败

检查 bucket、endpoint、region、访问权限、系统时间和预签名 URL 有效期。确认数据库中的 `file_path` 是对象 key，而不是本地绝对路径。

### worker 没有处理任务

```bash
systemctl status truckersmp-media-worker.timer
sudo -u www-data .venv/bin/python media_worker.py
```

## 20. 更新发布流程

```bash
cd /var/www/truckersmp-report-site
sudo systemctl stop truckersmp-report-site
sudo -u www-data git pull --ff-only origin main
sudo -u www-data .venv/bin/pip install -r requirements.txt
sudo -u www-data .venv/bin/flask --app manage.py db upgrade
sudo -u www-data .venv/bin/python check_migrations.py
sudo systemctl daemon-reload
sudo systemctl start truckersmp-report-site
sudo systemctl status truckersmp-report-site
```

更新前必须先备份数据库；不要使用 `git reset --hard` 覆盖服务器上的环境文件或上传目录。

## 21. 安全检查清单

- [ ] `ENVIRONMENT=production`。
- [ ] `SECRET_KEY` 为随机长值，不能使用示例值。
- [ ] `AUTO_CREATE_DB=0`。
- [ ] `REQUIRE_FFPROBE=1`、`REQUIRE_REAL_MIME=1`。
- [ ] `API_REQUIRE_BEARER=1`。
- [ ] metrics 使用独立 Token 且限制内网来源。
- [ ] Nginx 使用 HTTPS，Cookie Secure 已开启。
- [ ] 视频目录不可被公网直接访问。
- [ ] 数据库、视频/S3、`.env` 都有备份策略。
- [ ] cleanup 和 media worker timer 已启用。
- [ ] 已完成一次真实恢复演练。
